extends layout

block content
  div.oauth-box
    form(method='post')
      h3 应用授权
      hr 
      p #{loginUserId}，你确定要授权给应用 #{appInfo.name} 吗？
      p.text-muted 应用简介：#{appInfo.description}
      hr
      button(type='submit', class='button-primary') 确认授权
